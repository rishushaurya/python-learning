x = 3.14
y = 4
z = 5

# result = round(x)       # Rounds x to nearest integer
result = abs(y)         # Absolute value of y
# result = pow(4, 3)      # 4 raised to the power of 3
# result = max(x, y, z)   # Largest among x, y, z

#result = min(x, y, z)   # Smallest among x, y, z

print(result)

