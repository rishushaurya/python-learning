import math

radius=float(input("ENTER THE RADIUS OF THE CIRCLE :"))
c=(2)*(math.pi)*(radius)
a=(math.pi)*pow(radius , 2)
print(f"the circumference of the circle is = {round(c , 2)}")
print(f"the Area of the circle is = {round(a , 2)}") #rounds to 2 digit